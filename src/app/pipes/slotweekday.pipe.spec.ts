import { SlotweekdayPipe } from './slotweekday.pipe';

describe('SlotweekdayPipe', () => {
  it('create an instance', () => {
    const pipe = new SlotweekdayPipe();
    expect(pipe).toBeTruthy();
  });
});
