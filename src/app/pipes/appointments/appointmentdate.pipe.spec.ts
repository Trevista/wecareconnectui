import { AppointmentdatePipe } from './appointmentdate.pipe';

describe('AppointmentdatePipe', () => {
  it('create an instance', () => {
    const pipe = new AppointmentdatePipe();
    expect(pipe).toBeTruthy();
  });
});
